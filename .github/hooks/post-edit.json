{
  "hooks": {
    "PostToolUse": [
      {
        "type": "command",
        "command": "[ -n \"$TOOL_INPUT_FILE_PATH\" ] && npx prettier --write \"$TOOL_INPUT_FILE_PATH\" && npx eslint --fix \"$TOOL_INPUT_FILE_PATH\" || true",
        "timeout": 15
      }
    ]
  }
}
