export interface Artist {
    id_artiste: string;
    label: string;
}

export interface ArtistResult {
    artist: string;
    artiste?: string;
    id_artiste: string;
    id_artiste_deezer: string;
}